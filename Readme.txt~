###Dependencies######### Numpy >= 1.3, SciPy >= 0.7

All the file having py extension are the code used for Semi Supervised Regression (SSR). The reason to have different SSR codes is due to different data heading.

For the support vector regression you should use the file having _svr.py files



#############Data################# The data is in data folder.

There are 5 files in this folder used in the study. The Boston Housing data is from the scikit package.
